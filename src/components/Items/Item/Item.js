import React from 'react';
import useStyles from './styles';

const Item = () => {

    const classes = useStyles();

    return (
        <div>
            <h1>ITEM</h1>
        </div>
    );
};

export default Item;